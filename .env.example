CLOUDFRONT_DISTRIBUTION_ID=

# Google Analytics 4 (public ID; set at build time — e.g. Vercel env or local .env)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-W3GC6BXJ5Z

# Force noindex on any host (local preview, etc.)
PUBLIC_NOINDEX=false

# Only on Vercel: set to "true" if you explicitly want *.vercel.app indexed (default is noindex)
# PUBLIC_ALLOW_VERCEL_INDEXING=true
